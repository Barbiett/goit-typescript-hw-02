export default function ErrorMessage() {
  return (
    <p
      style={{
        color: "red",
        textAlign: "center",
        fontSize: "30px",
      }}
    >
      Oops! Error! Please reload the page!
    </p>
  );
}
